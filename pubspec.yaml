name: learn_progress_tracker
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  fl_chart: ^0.66.2
  intl: ^0.20.2
  shared_preferences: ^2.2.0
  provider: ^6.1.1
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  uuid: ^4.3.3
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
  generate: true 